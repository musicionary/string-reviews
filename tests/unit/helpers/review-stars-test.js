import { reviewStars } from 'string-reviews/helpers/review-stars';
import { module, test } from 'qunit';

module('Unit | Helper | review stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewStars([42]);
  assert.ok(result);
});
